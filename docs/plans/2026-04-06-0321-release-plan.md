# 0.3.2.1 Release Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Publish `0.3.2.1` as a release-only follow-up that fixes packaged Python runtime portability, updates versioned docs, and rebuilds the macOS installers.

**Architecture:** Keep the existing Tauri `.app + .dmg` release path. Tighten the Python bundling step so the packaged backend no longer depends on user-installed Python framework paths, then bump the version surface and regenerate both architecture installers.

**Tech Stack:** Node.js scripts, Tauri 2, macOS packaging tools, bundled Python runtime, repo release docs.

---

### Task 1: Lock the remaining runtime portability regression with tests

**Files:**
- Modify: `frontend/tauri/test/python-runtime-portability.test.mjs`

**Step 1: Write the failing test**

- Assert that bundled helper scripts under `src-tauri/resources/backend/python/bin/` do not keep build-machine shebangs.
- Assert that `pyvenv.cfg` does not mention the staging path.

**Step 2: Run test to verify it fails**

Run: `node --test frontend/tauri/test/python-runtime-portability.test.mjs`

Expected: FAIL because current helper scripts still point at the local `python.staging` path.

**Step 3: Write minimal implementation**

- Update the Python bundling script so it rewrites helper script shebangs and normalizes `pyvenv.cfg`.

**Step 4: Run test to verify it passes**

Run: `node --test frontend/tauri/test/python-runtime-portability.test.mjs`

Expected: PASS

### Task 2: Bump the release version surface to 0.3.2.1

**Files:**
- Modify: `package.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `backend/presto/main_api.py`
- Modify: `packages/contracts/package.json`
- Modify: `packages/sdk-core/package.json`
- Modify: `packages/sdk-runtime/package.json`
- Update any directly referenced version strings in release-facing docs

**Step 1: Update source-of-truth versions**

- Change all current `0.3.2` app/runtime version values to `0.3.2.1`.

**Step 2: Verify version references**

Run: `rg -n '0\.3\.2|v0\.3\.2' README.md CHANGELOG.md docs package.json src-tauri/Cargo.toml backend packages -g '!docs/plans/**'`

Expected: only intentional historical references remain.

### Task 3: Refresh release documentation

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `README.md`
- Create: `docs/releases/v0.3.2.1-release.md`
- Modify: release-facing docs that still describe the current baseline as `0.3.2`

**Step 1: Update release summary**

- Add a concise `0.3.2.1` changelog entry focused on packaged runtime portability and installer refresh.

**Step 2: Update README baseline**

- Point README "latest release" and version baseline to `0.3.2.1`.

**Step 3: Draft release note**

- Record the packaging fix, artifact names, and any release-specific notes.

### Task 4: Rebuild installers and verify packaged dependencies

**Files:**
- Build output: `release/tauri/arm64/*`
- Build output: `release/tauri/x64/*`

**Step 1: Build arm64 installer**

Run: `npm run tauri:build:arm64`

Expected: fresh `Presto_0.3.2.1_arm64.dmg` and matching `.app`.

**Step 2: Build x64 installer**

Run: `npm run tauri:build:x64`

Expected: fresh `Presto_0.3.2.1_x64.dmg` and matching `.app`.

**Step 3: Verify packaged Python runtime**

- Check `otool -L` for packaged `python3`.
- Check bundled helper scripts and config files.
- Confirm the packaged app now includes the vendored Python framework resources.

### Task 5: State the incremental installer conclusion

**Files:**
- No code required unless release notes need one sentence

**Step 1: Inspect current release stack**

- Confirm whether the repo now contains any real delta update / incremental installer mechanism.

**Step 2: Report exact conclusion**

- If unsupported, say so directly and explain why based on current release tooling.
